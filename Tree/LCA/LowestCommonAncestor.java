package Tree.LCA;

import DataStrucuture.TreeNode;

public class LowestCommonAncestor {
    /*
      class ReturnType {
        int found;//number of target in subtree
        TreeNode lca;
        public ReturnType(TreeNode lca, int found){
            this.lca=lca;
            this.found=found;
        }
    }

    public ReturnType computeLowestCommonAncestor(TreeNode root, TreeNode p, TreeNode q){
        if (root == null) {
          return  new ReturnType(root,0);
        }
        ReturnType rt;
    }
    */
}
